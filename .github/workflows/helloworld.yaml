name: "hello world workflow"
on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs_on: ubuntu-latest
    steps:
      -name: "print hello world"
       run: echo "hello world"



    
